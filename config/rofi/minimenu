configuration {
    modi: "drun,run,ssh";
    show-icons: false;
    display-drun: "Search";
    drun-display-format: "{name}";
    width: 100%;     
    lines: 5;
}

* {
    background-color: #000000;        /* Very dark background */
    text-color: #e0e0e0;              /* Soft white text */
    accent-color: #c9b03e;                
    highlight-color: #2e2e2e;         
    font: "UDEV Gothic HSLG 12";
}

window {
    background-color: @background-color;
    /* border: 2px solid @accent-color;  Sleek border */
    border-radius: 0px;
    padding: 0;
    
    anchor: north;
    location: center;
    
    children: [ mainbox ];
}

mainbox {
    orientation: vertical;
    children: [ inputbar, listview ];
    padding: 8px;
    spacing: 8px;
}

inputbar {
    orientation: horizontal;
    background-color: @background-color;
    border-radius: 0px;
    padding: 4px;
    spacing: 6px;
    children: [ entry ];
}

entry {
    background-color: transparent;
    text-color: @text-color;
    expand: true;
    /* placeholder: "Type to search..."; */
    padding: 2px;
}

listview {
    layout: horizontal;
    spacing: 4px;
    cycle: false;     
    scrollbar: false; 
    border: none;
    padding: 0;
}

element {
    padding: 4px 6px; /* Minimal padding for entry */
    border-radius: 0px;
    background-color: transparent; /* Inherit from window */
    border: none;
}

element selected {
    background-color: @accent-color;
    text-color: #1a1a1a; /* Dark text on gold */
}

element-text, element-icon {
    background-color: inherit;
    text-color: inherit;
    padding: 0;
    horizontal-align: 0.0; /* Align text to the left */
}
